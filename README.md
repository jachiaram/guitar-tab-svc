# guitar-tab-svc
A service built using fastapi in python for converting audio files to midi, and midi to guitar tabs / transcriptions

## Set Up
- Install python 3.9.7 if not already installed
- Create a virutal environment using that version
- activate the environment and install the required dependencies with the following command:
``` pip install -r requirements.txt ```

